#include<stdio.h>
int kiem_tra_so_lon_nhat (int n){
	int a[i];
	int max = a[0];
	for(i=0;i<n;i++)
	if (max < a[i])
		max = a[i];
		
		return max;
}

int main(){
	int a[i]={1,2,3,4,5,6};
	int n;
	printf("so lon nhat trong mang la:%d\n",n,kiem_tra_so_lon_nhat(n));
	
	return 0;
}
